# 온다(OnDa) 서비스 AI/LLM 로드맵 및 확장 계획

본 문서는 서버가 없는 완전한 클라이언트 단(로컬 IndexedDB 및 Vite PWA) 환경에서 사용자의 반려동물 건강 데이터(음수량, 대변 상태, 산책 시간, 일지 등)를 안전하게 분석하고 최적의 건강 피드백을 제공하기 위한 LLM(대형 언어 모델) 도입 및 확장 기획을 기술합니다.

---

## 1. 확장 목표
* **서버리스 AI 피드백 구현**: 고가의 클라우드 서버 인프라 구축 및 유지 관리 비용 없이, 클라이언트 환경에서 프라이버시가 완벽히 보호되는 맞춤형 AI 케어 리포트 작성.
* **데이터 기반 개인화**: 단순 고정 룰 기반 가이드를 넘어, 각 반려동물의 고유 정보(나이, 몸무게, 품종, 역사적 일일 기록)를 유기적으로 조합하여 개별 맞춤 솔루션 제공.
* **오프라인 및 로컬 퍼스트**: 모바일 PWA 환경에서의 원활한 작동 및 보호자의 민감 정보 유출 방지.

---

## 2. 핵심 아키텍처 및 구현 방안

### 📌 방안 A: 클라이언트 측 Gemini API 연동 (단기 목표 - 권장)
사용자가 직접 Google AI Studio에서 무료 Gemini API 키를 발급받아 설정 창에 저장하고, 브라우저가 직접 Gemini 서버로 분석 요청을 날리는 구조입니다.

* **구현 흐름**:
  1. **설정창 보완**: 설정 페이지 내 `Gemini API 키 등록` 및 `API 상태 검증` 필드 구현.
  2. **컨텍스트 수집 (Context Injection)**: 
     * IndexedDB 내에서 선택된 반려동물의 프로필(`Pet` 스키마) 정보 로드.
     * 최근 3~7일 간 기록된 건강 이벤트(음수량 달성도, 대변의 묽기 분포, 산책 시간 만족도, 증상 태그)를 하나의 JSON 형식 텍스트로 가공.
  3. **프롬프트 템플릿 작성**: 
     * 환각 방지를 위한 제약 조건("의학적 진단이 아님을 명시", "기록된 데이터에만 기반하여 조언")을 명시한 시스템 프롬프트(System Prompt) 설계.
  4. **직접 fetch 호출**: 브라우저 단에서 직접 `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent` 엔드포인트로 API 키를 실어 호출.
  5. **UI 렌더링**: 대시보드 홈 화면에 **"AI 전담 비서의 오늘의 건강 조언"** 섹션을 카드로 렌더링하여 친절하게 노출.

---

### 📌 방안 B: 브라우저 내부 로컬 RAG 시스템 구축 (중장기 목표)
사용자가 작성한 일기 및 이상 증상 기록이 장기간 누적되어 수만 토큰을 초과할 때, 관련성이 높은 과거의 기록들만 선별하여 프롬프트에 주입하는 벡터 데이터베이스 구조를 브라우저 내부에 구축합니다.

* **구현 흐름**:
  1. **로컬 임베딩 연산**: `Transformers.js`를 로드하여 경량 임베딩 모델(예: `all-MiniLM-L6-v2`)을 브라우저 가속으로 실행.
  2. **임베딩 데이터 저장**: 보호자가 다이어리(일기)를 작성/수정할 때마다 해당 내용을 실시간으로 벡터화하여 IndexedDB 내 `embeddings` 테이블에 코사인 유사도 연산용 벡터 데이터로 저장.
  3. **질의 매칭**: 현재 발생한 건강 이슈(예: "피부를 가려워함")와 가장 높은 코사인 유사도를 가진 과거 일지 기록들만 로컬에서 Top-K 검색으로 복원.
  4. **컨텍스트 필터링**: 복원된 과거 맥락만 LLM 프롬프트에 동봉해 최종 분석 피드백 생성.

---

## 3. 화면 설계 및 UX 요구사항

1. **설정(Settings) 화면 개편**:
   * API Key 입력 필드 및 눈모양(토글) 아이콘.
   * `무료 API Key 발급받기` 외부 가이드 링크.
   * 저장 버튼 및 연결 테스트 로딩 인디케이터.
2. **대시보드 메인 화면 개편**:
   * 메인 탭에 **"AI 맞춤 건강 브리핑"** 전용 스와이프 카드 배치.
   * 로딩 시 강아지/고양이 발자국 애니메이션 효과 적용.
   * 텍스트 클립보드 복사 및 가이드 내용 내 캘린더 바로가기 단추 제공.
3. **가이드(Guide) 상세 화면 연동**:
   * 현재 정적 가이드라인 구조에서 탈피하여, AI 팁 카드에 "AI 추천" 뱃지를 붙이고 동적으로 생성된 줄글 답변을 결합하여 노출.

---

## 4. 확장 로드맵 일정
* **Phase 1 (Gemini API 기반 컨텍스트 인젝션)**: 설정 화면 내 API Key 관리 스토어 및 오늘 날짜 기준 데일리 건강 피드백 기능 개발.
* **Phase 2 (하이브리드 룰 엔진 고도화)**: 기존 정적 가이드 규칙 결과값과 LLM의 텍스트 생성을 병합하여 답변의 임상적 신뢰성 검증 레이어 확보.
* **Phase 3 (Transformers.js 로컬 PWA 구현)**: 브라우저 캐싱을 이용해 오프라인에서도 돌아가는 모바일 로컬 AI 에이전트 실험 및 배포.
