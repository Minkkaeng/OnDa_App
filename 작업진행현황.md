# 🐾 OnDa Pet Care (온다) 작업 진행 현황

## 📌 1. 프로젝트 개요
* **앱 명칭**: OnDa Pet Care (반려동물 맞춤 케어 & 회복일지)
* **주요 기능**: 온보딩 프로필 작성, 데일리 케어 루틴 체크리스트, 약/영양제 30일 복용 스케줄러, 질병 회복일지 및 수의사 상담 리포트, 전역 산책 타이머, 캘린더 일지 관리
* **기술 스택**: React 18, TypeScript, Vite 8, Zustand (전역 상태), Dexie.js (IndexedDB 로컬 DB), Lucide Icons, Vite PWA, Capacitor

---

## 📊 2. 진행 상태 종합 현황

### ✅ DONE (완료된 핵심 작업 내역)

#### 1) 🔄 신규 온보딩 진입 시퀀스 개편
- **진입 순서 정상화**: `스플래시(Splash 3.5초) ➔ 글로벌 가이드 투어(Global Tour) ➔ 시작하기(웰컴 모달) ➔ 약관동의/알림설정(Step 0) ➔ 초기 프로필 작성(Step 1~3)` 흐름 완비.
- **약관 및 알림 중앙 모달 레이어 개편**: 필수 약관 2종(위치기반 서비스 이용약관, 개인정보 처리방침) 및 선택 알림 수신 동의 체크박스를 프리미엄 중앙 모달 레이어 팝업 형태로 개편. 전체 동의 버튼 및 '동의하고 계속하기' 완료 시 프로필 작성 단계로 매끄럽게 연결.
- **프로필 작성(Step 1~3) UX 고도화**:
  - 12시간제 (오전/오후, 시, 분) 휠 스크롤 시간 피커 모달 연동.
  - 질병 데이터셋 50종 기반 실시간 자동완성 검색 드롭다운 연동.
  - 알레르기 칩 가로 스크롤 뷰(`horizontal-scroll-chips`) 탑재.
  - 하단 버튼 반응형 처리 (Step 0 시 100% 가로 너비의 '동의하고 계속하기' 단독 버튼 활성화).

#### 2) 📱 안드로이드 가상 키보드 수직 가림/짤림 버그 해결
- **Viewport 반응형 확장**: `visualViewport.addEventListener('resize', ...)` 감지 기반 키보드 팝업 시 포커스된 `input`으로 자동 뷰포트 스크롤 이관.
- **레이아웃 구조 보정**: `.keyboard-active` 시 Flex 수직 중앙 정렬 해제 및 `.onboarding-card` 내 약관 영역 분리로 키보드 활성화 시 카드 하단이 잘리는 문제 원천 해결.

#### 3) 🏥 Care 탭 - 데일리 루틴 & 약 복용 스케줄 연동
- **수직 타임라인 체크리스트**: 시간대별 아침식사, 약 복용, 산책, 저녁식사, 건강점검 순차 체크 및 로컬 저장.
- **3단계 간편 건강 점검**: `양호` / `주의` / `경고` 3단계 상태 칩 선택 및 한 줄 특이사항 작성 후 캘린더/일기장 자동 저장.
- **30일 약 복용 스케줄러**: 약 이름, 복용 주기(매일 또는 N일마다), 복용 시간을 입력하면 향후 30일치 일정을 캘린더에 일괄 자동 등록.

#### 4) 🩺 Diary 탭 - 질병 회복일지 & 수의사 상담 리포트
- **서브 탭 분리**: `일상 기록` vs `질병 회복일지` 서브 탭 분류 네비게이션.
- **회복일지 타임라인 피드**: 날짜별 체온(🌡️), 처방약 복용 여부(💊), 환부 1:1 사진(크롭 연동), 경과 메모 기록.
- **수의사 상담 리포트 인쇄/PDF 저장**: 회복 일지 데이터를 집계하여 깔끔한 모달 출력 문서 생성 및 `window.print()`를 통해 실물 인쇄/PDF 저장 지원.
- **FAB버그 해결**: 플러스(+) 작성 버튼 `zIndex`를 `999`로 조정하여 하단 네비게이션 및 리스트 뒤로 숨겨지는 문제 해결.

#### 5) 🧹 폴더 구조 정돈 및 컴포넌트 모듈화 리팩토링 (Clean Architecture)
- **`components/common/`**: `ScrollTimePickerModal.tsx` 공통 휠 스크롤 시간 선택기 추출.
- **`components/onboarding/`**: `OnboardingStep0Terms`, `OnboardingStep1Profile`, `OnboardingStep2Routine`, `OnboardingStep3Health`, `OnboardingTermsModal` 5개 서브 컴포넌트 분리.
- **`components/diary/`**: `DiaryHeaderTabs`, `DiaryDailyList`, `DiaryRecoveryTimeline`, `VetConsultReportModal`, `DiaryFormModal` 5개 서브 컴포넌트 분리.
- **`components/care/`**: `DailyRoutineTimeline`, `QuickHealthCheck`, `MedicationScheduler` 3개 서브 컴포넌트 분리.
- **페이지 파일 슬림화**:
  - `Onboarding.tsx` (1,680라인 ➔ **750라인**)
  - `Care.tsx` (1,200라인 ➔ **170라인**)
  - `Diary.tsx` (1,100라인 ➔ **400라인**)

#### 6) 📄 API 연동 가이드 문서화
- `docs/hospital_api_guide.md` 신설: 카카오 Local API 및 다음 도로명 주소 검색 연동 가이드 및 예시 코드 작성 완료.

#### 7) 🎨 앱 전체 페이지/컴포넌트 UI/UX 디자인 시스템 통일화
- **공통 디자인 토큰 및 클래스 탑재 (`src/index.css`)**:
  - `.onda-page-container` (샌드 베이지 `#F8F7F3` 배경, 100% 모바일 뷰포트 레이아웃)
  - `.onda-card` (`#FFFFFF` 카드리폼, `16px` 라운딩, `0 4px 16px rgba(0,0,0,0.04)` 그림자)
  - `.onda-input` (`42px` 높이, `24px` 알약 라운딩, `#FCFAF7` 배경, `1.5px` 스트로크 border)
  - `.onda-btn-primary` (`48px` 높이, `14px` 라운딩, `800` 폰트, `#5C715E` 메인 컬러)
  - `.onda-section-title` (`1.35rem`, `800` 폰트, 수직 중앙 정렬 아이콘)
- **전체 뷰 통일 적용**: `Dashboard`, `Care`, `Calendar`, `Diary`, `Profile`, `Settings` 6개 주요 화면에 100% 동일한 C-Option 디자인 시스템 매핑 완료.

#### 8) 🗺️ 위치 기반 주변 동물병원 & 동물약국 지도 연동 (`HospitalMap.tsx` & `PostcodeModal.tsx`)
- **신규 뷰 개설 (`src/pages/HospitalMap.tsx`)**:
  - 사용자 GPS 위치 감지 및 Daum 도로명 주소 검색 모달(`PostcodeModal.tsx`) 연동.
  - 카카오 Local API 기반 주변 3km 이내 동물병원, 24시 응급실, 동물약국, 애견미용실 실시간 조회 및 필터 탭 제공.
  - 전화 걸기(`tel:`), 카카오 지도 보기 링크, 거리(km) 정보가 들어간 전용 카드 레이아웃 구현.
  - 대시보드 메인 퀵 배너 ("내 주변 동물병원 & 약국 찾기 🏥") 탑재.

#### 9) 🔔 Capacitor 네이티브 모바일 알림 연동 (`notificationService.ts`)
- **알림 서비스 모듈 개발 (`src/services/notificationService.ts`)**:
  - `@capacitor/local-notifications` 플러그인 연동으로 30일 약/영양제 복용 시각 및 매일 산책 타임라인 네이티브 푸시 알림 예약 기능 구현.
#### 10) 🚀 사용자 흐름(User Journey) 최적화
- **스플래시 속도 개선**: Splash 렌더링 시간을 3.5초 ➔ **1.8초**로 단축하여 체감 앱 진입 속도 향상.
- **온보딩 간소화**: 첫 실행 시 `스플래시 ➔ 중앙 약관 동의 모달 ➔ 초기 프로필 작성 ➔ 대시보드` 직관적 흐름 완성.
- **대시보드 퀵 타일 내비게이션 연결**: 대시보드의 일기/산책/의료/스케줄 타일 클릭 시 관련 전용 탭(`Diary`, `Care`, `HospitalMap`, `Calendar`)으로 즉시 이동(Deep Navigation).
- **투약 스케줄러 등록 후 동선 개선**: `Care` 탭 30일 약 복용 스케줄러 생성 시 즉시 `캘린더` 탭으로 이동할 수 있는 스마트 확인 다이얼로그 추가.
- **데일리 케어 달성률 뱃지**: `DailyRoutineTimeline`에 실시간 케어 달성률 % 및 `100% 달성 🎉` 뱃지 추가.

---

## 🔮 3. 다음 작업 시 즉시 이어서 진행할 내역 (ACTIONABLE NEXT STEPS)

### 📌 Long-term (향후 확장 과제)

1. **☁️ 백엔드 클라우드 데이터 동기화 & 회원가입**: Dexie IndexedDB 로컬 데이터를 Supabase / Firebase 클라우드 DB와 연동하여 소셜 로그인 및 데이터 복구 구현.
2. **💡 AI 기반 맞춤형 건강 팁 엔진**: 품종, 나이, 체중, 기저 질환 정보를 바탕으로 AI 맞춤 일일 케어 가이드 팁 노출.

---

## 🛠️ 4. 빌드 및 품질 검증 현황
- `npm run build` (`tsc -b && vite build`): **Pass (오류 0건, 빌드 타임 ~2.30초)**
- PWA SW Service Worker 파일 정상 구성 (`dist/sw.js`, `dist/manifest.webmanifest`)
