# 작업 규칙 정리

## 기술 스택
- **Core**: React 19, TypeScript, Vite
- **State Management**: Zustand
- **Local DB**: Dexie.js (IndexedDB)
- **Routing**: React Router DOM
- **Mobile Wrapper**: Capacitor
- **UI/Styling**: Vanilla CSS, Lucide React (Icons), Recharts (Charts)

## 코딩 컨벤션
- **컴포넌트**: 함수형 컴포넌트(`React.FC`) 및 React Hooks 사용
- **상태 관리**: 전역 상태는 `store` 디렉토리 내 Zustand 스토어(`usePetStore` 등)를 통해 관리
- **데이터베이스**: 로컬 데이터 처리는 `db` 디렉토리의 Dexie 스키마를 사용하여 CRUD 작업 수행
- **스타일링**: CSS 변수(`var(--main-primary)` 등)를 적극 활용하여 테마 변경(Light/Dark 등)에 유연하게 대응

## 브랜치 및 커밋 전략
- **커밋 메시지**: `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore` 등의 Prefix 사용
  - 예시: `feat: 반려동물 프로필 추가 기능 구현`
- **브랜치**: 기능별(feature/) 또는 버그 수정(bugfix/) 브랜치를 생성하여 작업 후 메인 브랜치로 병합

## 문서화 가이드
- 주요 기능 추가 시 해당 기능의 의도와 로직을 주석으로 명확히 남길 것
- 프로젝트 진행 현황 및 상세 분석 내용은 루트 디렉토리의 마크다운(MD) 파일 최신화 유지
